pipeline { 
    agent any
    environment {
        DOCKERHUB_CREDENTIALS = credentials('dockerhub_id')
    }
     stages {
        stage('Build Docker Image') {
            steps { 
                sh 'docker build -t newimage:2.0 .'
                }
            }
        stage('login to dockerhub'){
            steps {
            sh 'echo docker login'
            }
        }

        stage('push image'){
            steps {
                sh 'echo docker push newimage:2.0'
            }
        }
    }
         post { 
            success { 
                echo "Build is successfull" 
            } 
            failure { 
                echo "Build failed" 
            }
        }
    }
